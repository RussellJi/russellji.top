---
title: IO模型
description: "大白话解释同步异步阻塞非阻塞,以及常见的IO模型"
date: April 2 2022
---

# IO 名词解释
## 同步异步

根据谁是主动发起IO请求的一方来判断:
- 同步:==用户线程主动==发起IO请求,内核被动接收
- 异步:==内核主动==发起IO请求,用户线程被动接收

## 阻塞非阻塞

根据调用线程的状态来判断:
- 阻塞:调用线程==要等待==被调用者的返回结果才可进行其他操作
- 非阻塞:调用线程==不用等待==被调用者的返回结果就可进行其他操作
